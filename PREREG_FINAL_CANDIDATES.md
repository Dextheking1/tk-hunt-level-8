# Final live candidates — decision record (2026-09-05)

All grid-mechanism derivations are dead (exhaustive, background-controlled;
see HIDDEN_FINDINGS.md burn list). All image-content derivations submitted so
far are INVALID. This document ranks the remaining live, never-submitted,
chain-endorsed candidates with their full derivations, for operator
submission (1/min, free, no strikes).

## 1. `block grid thingy`  ← SUBMIT FIRST

Derivation (< 5 min, hand-redoable):
1. The L8 page's only multi-word text is the h2 title "BLOCK GRID THINGY".
2. House format = exactly 3 unconnected common English words, lowercase,
   space-separated — "block", "grid", "thingy" qualify (same looseness as
   house answers pump/premium).
3. Official rule (tk-clues): "Try not to overthink it." The title link is a
   banana-trivia joke video (placeholder) — nothing behind it.
4. Board corroboration (verified this pass, Boggle DFS on the audited grid):
   **GRID = 4743 has exactly 1 path** and its anagram **GIRD = 4473 also has
   exactly 1 path** — rare unique-path plantings among ~1,650 board words.
5. The site's own L8 background file is named **Gird_Background.jpg** — the
   author independently chose the board's unique-path anagram word for the
   page furniture.
6. "Block grid" literally describes the board (a Boggle block grid); the
   forensics chain is the depth-misdirection.
Never submitted on any log; top of the swarm's untried queue; no recorded
control kills it. (Operator's "titles excluded" ruling targeted song
titles/phrases — Papa/Isla/i-love-you, all INVALID — not the page's own name.)

## 2. `call touch reach` + permutations  ← if #1 fails

Cover-lyric contact verbs, one per instruction cover: Ghostbusters
"who you gonna **call**", Personal Jesus "**reach** out and **touch** faith",
Papa "don't P-**reach**", Stevie "I just **called**" → the three mechanism
verbs are call / touch / reach (the swarm's covers-as-instructions
framework). Not board-confirmed (T9 codes 2255/86824/73224 absent), order
underdetermined → try: `call touch reach`, `touch call reach`,
`reach call touch`, then the remaining 3 permutations.

## 3. `two zero seven`  ← if #2 fails

Digit sum of the 42-cell grid = 207 (rows 30+37+42+27+31+40). "Letters and
numbers are fair game." Trivial hand derivation; weak on merit (207 is
unremarkable vs mean 210).

## Decision rule

- Submit in order at 1/min; wrong submits never strike.
- Any INVALID on #1: the page-title class dies; move to #2, then #3.
- If #1–#3 all fail: the answer requires the unseen 10th image (the 19.3 KB
  encrypted payload in image07). Only unmined surface left = the four
  member-gated /twilio/{Ash,Aiden,Jess,Morgan} pages in a logged-in browser
  (guest fetch = name only, empty body), plus a full Q&A scroll (admin
  answer to "What does the number 1 represent?").

## Rejected alternatives (this decision)

- `ring my bell` — the fifth 3-word song title; titles are 0/4 as answers
  (papa ×3, isla, i-just-called all INVALID) and operator-excluded.
- `tills smelt kelli` — planted-chain order; background control put the
  straight-line word set at chance (4 vs 9.4 random average).
- `see you again` — phrase-shaped title; operator format excludes titles.
- Any grid-mechanism triple — the entire mechanism well is dry
  (HIDDEN_FINDINGS.md).
