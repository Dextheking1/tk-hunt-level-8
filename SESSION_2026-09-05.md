# Session 2026-09-05 — status, closures, and one new closed family

## Bottom line
Level-8 final answer (3 random English words) remains **unsolved**. No new
submittable candidate was produced. This session closed several documented
open items, validated the image07 seed-mapping hypothesis from in-repo
anchors, and ran one more bounded password family that missed.

## 1. MD5-XOR-fold seed mapping — validated from in-repo anchors
Follow-up C in `PREREG_STEGHIDE_SEED_MAPPING.md` was preregistered but its
synthetic-carrier validation could not be executed here (no `steghide`/
`stegseek` binary and no way to build one: apt mirrors and GitHub release
assets are blocked by sandbox egress; only pypi/github-API/codeload/raw are
reachable, and no jpeg/mhash/mcrypt dev headers exist).

The hypothesis can nevertheless be checked against every (passphrase, seed)
pair already recorded in the repo. Define

    H(p) = LE32( MD5(p)[0:4] XOR MD5(p)[4:8] XOR MD5(p)[8:12] XOR MD5(p)[12:16] )

Both known anchors match exactly:

| passphrase | recorded StegSeek seed | H(p) | source |
|---|---|---|---|
| `planet`   | `2c52fe1c` | `2c52fe1c` ✓ | PREREG_STEGHIDE_SEED_MAPPING follow-up B control |
| `` (empty) | `3b75655e` | `3b75655e` ✓ | image04 blank-password payload (`PREREG_STEGSEEK_SEED`) |

Two independent 2^32 anchors agreeing makes accidental agreement ~2^-64.
The mapping is treated as **validated**, closing follow-up C on the available
evidence. Consequently image07's passphrase must satisfy

    H(p) == 0x0f58d719

and any future wordlist attempt on image07 should be gated by this 32-bit
preimage check (equivalent to what StegSeek wordlist mode does internally)
before any full extraction is attempted.

## 2. New closed password family (image07): musician catalog/full-name/theme set
Ran an H-preimage scan over **732 normalized variants** (empty/space/hyphen/
underscore/joined, case forms, CamelCase, punctuation-stripped) of:

- Orianthi full name and hometown (`orianthi panagaris`, `adelaide`);
- Orianthi albums/singles per public discography: violet journey, believe,
  believe ii, heaven in this hell, o, rock candy, some kind of feeling,
  live from hollywood, according to you, shut up and kiss me / & kiss me,
  courage, frozen, sex e bizarre, better with you, impulsive, sinners hymn,
  attention, dark days are gone, highly strung, bad news, whats it gonna be,
  missing you, think like a girl, now or never, light it up, where did your
  heart go, sorry, radio free america, livin(living) on a prayer, this is it,
  michael jackson this is it, girl in a catsuit, we are the world, prs,
  white prs, signature prs, orianthi hotline/payphone;
- full legal names of all identified subjects (Madonna Ciccone, Aubrey Drake
  Graham, Cameron Jibril Thomaz, Ray Parker Jr, Stevie Wonder/Stevland
  Morris, Dan(iel) Reynolds, Depeche Mode, Michael Jackson, Kyle Wood);
- site/title phrases (treasurekracken8, tk level 8, block grid thingy,
  kracken level 8, loveloud, utah, slc, …) and cover-title set with
  apostrophe variants.

**Result: 0 hits** (expected at ~1.7e-7 for 732 candidates over 2^32).
This family is now closed; do not re-run without a new derived rule. Full
candidate enumeration was inline/transient; the base-word lists are above.

## 3. Grid mechanism closures (code-verified, gibberish)
- 2x2 overlapping block sums/prods/|diffs|/xors over the 30 windows mapped to
  A–Z: `XAUPNOSAZTXYLUBZUVROQVRQXQLQXZ`, `YIIGQIGTSCGSEPCYRIUXDUKBYQSSMM`,
  `FEGHDCECFHJEDIJDIDFCEFHEDGHGHH`, `PMKBHAMGHFJGFMFPIDHCILHAPGFOPP`. Nothing.
- Row-pair 2-digit columns (10s/1s as 1–26): ~6 valid letters, no words.
- Row/col sums mod 12 (month letters): `FAGDGE` / `EJAKAA`. Nothing.
- `F341.zlib` mystery closed: it is exactly the grid.png IDAT stream carved at
  offset `0xF341` (`g[0xf341:…]` == file), i.e. compressed pixel data with the
  later IDAT chunk headers embedded, which is why plain zlib fails on it.
  No hidden content; `F341` (0 bytes) was the intended output name.

## 4. Site-structure facts recorded (static reads only, no submissions)
- Full `pageJsonFileNames` and page metadata were extracted from
  `level8.html` into `site_page_map.json` (48 pages). Level pages
  (k09pq…n235y), TOOL (`jyh0e`), TEST (`vriho`) have **no** static page JSON
  filename (code/protected pages). Password-protected pages:
  `jyh0e`(TOOL), `udtvc`, `ofig8`.
- twilio-page (`b0tok`, public, static JSON
  `c217b5_cb8798945434e13a8894de801094e340`) renders an **empty body**
  (`PAGE_SECTIONSb0tok` has zero components); it exists only as the `/twilio`
  router host (`routerFunctionName twilio_Router`, `twilio_SiteMap`). Its
  backend bundle is not exposed at any predictable bundler URL (tried
  `backend/twilio_Router.js` → 404; `pages/b0tok.js` → 500 = no page code).
- Bundler page-code endpoint only serves pages that actually have code:
  `pages/dve99.js` (the gate we already have) returns 200; all others
  probed returned 404/500. No further page code is fetchable this way.
- image12 GPS: EXIF has a GPS IFD pointer but it contains only GPSVersionID
  (no coordinates). Confirmed "no GPS" conclusion. Canon PowerShot SX410 IS
  capture, DateTime 2018-08-19 00:25:37, Windows Photo Editor 10 processed.
- JPEG marker walks on all carriers + nested covers: only JFIF/EXIF/SOF/DQT/
  DHT/SOS, entropy-coded tails; nothing new to carve.

## 5. Environment notes (not puzzle findings)
- `pip` (PyPI) works in this sandbox; apt mirrors, deb.debian.org, github
  release-assets and most parastorage hosts are blocked at TLS. `fetch_page`
  can reach bundler.wix-code.com and siteassets.parastorage.com.
- Installed here (not persisted): pillow, opencv-python-headless, pyzbar,
  numpy.
- No steghide/stegseek binary available; building is not feasible without
  dev headers/mirrors. Do not spend time re-attempting until the sandbox
  egress changes or a candidate passphrase needs verification elsewhere.

## 6. Puzzle state (unchanged, for the record)
- All live-gate submissions listed in ANALYSIS/HIDDEN_FINDINGS remain the
  complete INVALID set. T9/phone remains the best-supported but
  unconfirmed mechanism; every word-search/Boggle/PICDIGIT/covers reading
  tried so far is at or below chance or was falsified on the gate.
- The image07 payload (19.3 KB compressed, seed `0f58d719`) remains the only
  unextracted hidden content; its passphrase is not any tested generic/
  corpus/catalog family.

## Addendum (second pass, 2026-09-05) — inventory & site closure
- Straight-line T9 inventory re-derived exhaustively with a 60k wordfreq list,
  case-correct, all 8 directions, both modes:
  - pure T9, len>=5 commons: **HOMIE, SMELT, TILLS** only (dhoni = cricketer
    name, kelli = name — noise).
  - 1=I, len>=5 commons: adds **IDIOM, SHEIK, TAINT** (all three force the 1).
  No new word exists. Every selection/order of these (HST/HTS/SHT/STH/THS/TSH
  and IST/ITS/SIT/STI/TIS/TSI permutations) is already INVALID on the gate.
  Conclusion hardened: straight-line T9 words are NOT the answer; nothing on
  the 42-cell grid produces a novel submittable triple.
- Live public reads (fetch_page; no submissions): Q&A top threads confirm the
  admin statement that **Level 0 exists and "does give a little hint for later
  on in the game"**; no level-8 hint exists in Q&A. COWLAZARS and IG pages
  contain only giveaway/editorial leftovers ("Caveman Crowd Spear Fight",
  LOVELOUD-style tool text) — nothing puzzle-related. twilio-page body is
  empty (router-only). Level-0 itself is unlisted in current site code
  (page map has 48 pages, levels 1-11 only) and was never archived; its hint
  is currently unreachable without a live account/older sitemap.
- Wayback: 5 captures of /level-8 from 2025-09-07..09 (site launch era,
  pre-hunt). Not retrievable as raw JSON here; no archived copy of the grid
  media file found in CDX. Treat as placeholders; nothing lost if ignored.
- No new answer candidate produced this pass. Open items unchanged: image07
  passphrase (H preimage = 0f58d719), and the still-unknown non-T9 straight
  mechanism (or Boggle+selection rule) the author actually used.

## Addendum (third pass, 2026-09-05) — running scans & closures
- F36 preimage scan (a-z0-9 len 1-6, 2.24e9) confirmed single-core on length 6
  after the pool's other worker went idle (monitor blocks on next(it) so the log
  stalls at the 2.8% line; workers unaffected). Launched F62 (a-zA-Z0-9 len 1-5,
  0.931e9) in parallel on the idle core as process image07-f62-preimage-scan-38c1f261.
- New H-preimage wordlist family (tools/image07_wordlist_scan.py, fam=WLS,
  124,037 candidates): PRS/Paul Reed Smith orbit, full legal names w/ middle
  parts, record labels, cover titles WITH articles, image07 visual vocabulary,
  L6/L7 known answers, puzzle vocabulary, odd tokens (questiclesdotnet, bananas,
  kylewood), years/hex literals; case/separator/affix transforms. 0 hits.
- Additional streaming H-preimage family (fam=WF60KTRANS): every wordfreq-60k
  word 3-14 chars x {lower,Capital,ALL,UPPER,Title} x {41 suffix strings incl
  years/leet/x/!/07..} + digit-prefix forms = 10,763,100 candidates; 0 hits.
  (Earlier materialized-set attempt OOM-killed; streamed version succeeded.)
- Fresh grid attempts this pass all garbage: multi-tap pair readings
  (key,press) H/V; whole-stream numerical transforms (decimal->bytes,
  pairs-as-ASCII, triple mod 26, base-9 digit-shift variants). No text.
- Straight-line pure-T9 inventory re-derived independently with paths
  (len>=3). Unique-path 4-letter commons (slow/plow share diag, melt, till,
  gong/hong share row, etc.) form no clean triple; no new ≥5 word exists.
- Level-7 calibration PNGs are plain photos (no QR/text); drop.
- OCR/vision unavailable in this sandbox (no tesseract, no vision); artist
  photo text (OVO/LOVELOUD etc.) cannot be re-read programmatically here.

## Addendum (third pass, results) — F36/F62 complete; one H-collision
- F36 (a-z0-9 len 1-6, 2,238,976,116 candidates) COMPLETE: **one H-hit
  `b'ketoxe'`** (md5 74cdf1d9c62ea32ccf202e646414249e; H fold = 0x0f58d719 ✓).
  Interpretation: expected random collisions over this sweep size against a
  2^32 target ≈ 0.52, so a single hit is the expected false-positive rate.
  `ketoxe` is not in the 60k wordfreq list and Stage B's real-extraction scan
  (a-z len<=6, controls passed) already proved no pure-lowercase <=6 string is
  the passphrase; therefore ketoxe is a collision, NOT the author passphrase.
  Verified by direct fold computation. Do not chase ketoxe.
- F62 (a-zA-Z0-9 len 1-5, 931,151,402 candidates) COMPLETE: 0 hits.
- Conclusion now: image07 passphrase is NOT alnum <=6 lowercase, NOT
  mixed-case alnum <=5, not pure decimal <=8, not any seed-form; and none of
  the semantic families (musician catalog 732, corpus 1.76M, generic 7.14M,
  top-5k word pairs 100M, WLS 124k + curly/reverse, WF60KTRANS 10.76M,
  WPAIRS 537k, WSYM 77k) contains it. Most likely shape: >=7 chars,
  or contains symbols/uppercase beyond the closed families, or an uncommon
  phrase. Any future brute-force hit MUST be treated as a probable collision
  (P≈ sweep/2^32) until a real `steghide extract -sf image07.jpg -p <hit>`
  validates it (binary unavailable in this sandbox).
- Non-scan closures this pass: row/col whole-line T9 words under 1=I (none),
  multi-tap pair readings, whole-stream numeric/base transforms, straight-line
  4-letter inventory (no clean triple), Level-7 calibration PNGs are plain
  photos, OCR/vision unavailable.

## Addendum (verification of relayed agent report, 2026-09-05)
Audited the other session's claims (commit 9283d34, pushed to origin/master by
arena/01a06b20; NOT on this branch - our tip stays ff23fd1). Fetched origin and
verified:
- image07_fast_fold_matches.tsv: 88 rows, all MD5-XOR-folds recomputed == 0x0f58d719
  (0 mismatches); hex/ascii consistent; extract rc=1 twice + output NONE on all 88.
- Family total 351,729,273,631 == 95^5+62^6+36^7+26^8 exactly; expected fold
  collisions 81.893; 88 observed (P>=88 ~ 0.26 Poisson) - statistically normal.
  Per-family: alnum6 15 (exp 13.2), lowernum7 16 (exp 18.2), lower8 57 (exp 48.6),
  printable5 0 (exp 1.8).
- ketoxe (our F36 unique hit) reappears in their alnum6 set: two independent
  exhaustive scans agree on the same unique collision - strong completeness
  cross-check of both runs and of the H-mapping.
- Control phrasing "{test}" is loose: H('test')=0xcb4257a3 != target, so it can
  only be an implementation-parity control (target=H('test')), not a target-seed
  collision set. Not contradictory, but imprecise as relayed.
- Payload claim stands on PREREG_STEGSEEK_SEED.md controlled seed-mode evidence
  (reproducible 0f58d719/19.3KB/rijndael-128-CBC; image04 positive, flat-JPEG
  negative). 176 extraction failures are consistent but neutral (any password
  fails on a payload-less carrier too); no steghide/stegseek binary exists in
  THIS sandbox to re-run. Carrier identity confirmed: sha256(image07.jpg) ==
  hidden/image07.jpg == 2059E.zip member == 605ef68348bda6cb...fe56819.

## Addendum (audit of seed-dispute adjudication, 2026-09-05)
Relayed claim: "payload side wins; counter-claim wrong; proven with the exact
control it skipped; logged and pushed f02ddfa." Verified against repo+server:
- origin/master tip = f02ddfa "adjudicate seed dispute: negatives miss, payload
  stands" (2026-09-05 04:35 +0200); parent chain 0dad359 -> ... -> 9283d34 ->
  edf65cf "independently verify image07 payload and fold oracle" -> f02ddfa.
  So "edf65cf->f02ddfa (my commits)" is exact; 6188fb4 is indeed the tip of
  arena/01a06b20 (the other session's branch), NOT master.
- 7ff5f1b: direct `git fetch origin 7ff5f1b` rejected ("couldn't find remote
  ref"), exit 128 -> does not exist on the server. Same for d69ca74. Nothing to
  push from here; no push performed.
- "rounds 5/6/7/7b": no such content anywhere in the master tree (git grep on
  f02ddfa; the only '7b' hits are hex suffixes in unrelated sha256 strings).
  Never on record - confirmed.
- Adjudication evidence in f02ddfa (MY_SOLVE_PASS.md addenda 14 + dispute
  note): stegseek --seed full 2^32 scorecard image04 HIT (seed 3b75655e,
  47.7 KB, rijndael-128/CBC) / image07 HIT (0f58d719, 19.3 KB, rijndael-128/
  CBC) / image02 MISS / fresh synthetic JPEG MISS (exit 1, no output).
  Consistent with the ORIGINAL preregistered controlled seed-mode record
  (PREREG_STEGSEEK_SEED.md): same seeds/metadata; image02, image06, image12 and
  a flat-JPEG negative all completed with "no valid seed"; image04 positive.
- Independent recompute here: H('planet')=2c52fe1c and H('')=3b75655e (both
  recorded seeds) reproduce exactly; all 88 TSV fold collisions == 0f58d719 (0
  mismatches under the f02ddfa tree). Method soundness: positives produce the
  seed equal to H(passphrase) of the known embedding; clean carriers never
  report seed+metadata. The counter-claim's capacity/steghide-info argument
  targets a different tool mode and does not address the seed-mode record.
- Caveats: no stegseek/steghide binary in THIS sandbox, so the adjudication
  runs (like the originals) are verified as committed records, not re-executed
  here. edf65cf's "7/7 doc samples" fold-match refers to follow-up-C synthetic
  carriers absent from the repo; only the two in-repo anchors are directly
  re-verifiable (both match). Net: the payload claim (encrypted steghide
  payload in image07, seed 0f58d719) remains the best-supported reading, and
  all repo-state corrections in the message check out exactly.

## Addendum (complete inventory map verified, 2026-09-05)
Consolidated verified inventory (each row cross-checked against committed
records this pass):

| file | deeper content | lock | evidence |
|---|---|---|---|
| grid.png | appended ZIP (3,146,517 B @0x2059e) + eXIf(62,220 B) cover (Personal Jesus) | none — carve/read | chunk walk; MY_SOLVE_PASS add.2 |
| image01.png | tEXt Artist -> base64 JPEG (Papa Don't Preach, 15,082 B) + iTXt XMP dc:date 1987-02-25 | none — base64 decode | chunk walk, iTXt dumped in full |
| image02.jpg | nothing | — | full 2^32 stegseek --seed negative (44.4 s); corpus/generic/mask sweeps negative |
| image04.jpg | Stevie Wonder cover; TRUE embedded name image11.jpg (47.7 KB, rijndael-128/CBC) | none — blank passphrase | steghide info -p '' reports name; re-extraction sha256 7643d732... == steg04.jpg |
| image06.jpg | nothing | — | seed scan negative (43.7 s); capacity 540 B, no key |
| image07.jpg | 19.3 KB compressed encrypted payload (seed 0f58d719, rijndael-128/CBC) | unknown passphrase | controlled seed-mode detection, reproducible; ~all bounded key families dead |
| image12.jpg | nothing | — | seed scan negative (44.2 s) |
| 3 nested covers | artist_hidden.jpg / steg04.jpg / exif_personaljesus.jpg terminal | — | all 9 JPEGs: EOI at EOF-2, ZERO trailing bytes, no COM/nonstandard APPn; outguess "extractions" proven false positives |

Consequence: image11 exists only as image04's embedded payload filename, NOT a
ZIP entry, so the numeric sequence 01..12 still lacks real files 03, 05, 08,
09, 10 (PIC_DIGIT_BREAKTHROUGH reads 08-10 = TWO). Note the map's "3 nested
covers" = Papa Don't Preach (in image01), Stevie/image11 (in image04),
Personal Jesus (in grid.png eXIf); all terminal, nothing further to carve.
No new payload surface exists beyond image07's encrypted bytes.
